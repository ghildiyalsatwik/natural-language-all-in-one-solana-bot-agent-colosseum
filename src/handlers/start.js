export const startHandler = (userId) => {

    console.log(`Start handler called by userId: ${userId}`);

    const reply = 'Welcome to the all-in-one Solana bot! What can I do for you?';

    return reply;
};